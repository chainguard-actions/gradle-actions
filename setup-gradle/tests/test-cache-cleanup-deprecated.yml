name: Test gradle-home-cache-cleanup deprecated input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: setup
        uses: .
        with:
          gradle-home-cache-cleanup: true
          cache-disabled: true
          validate-wrappers: false
      - name: Assert setup completed
        run: |
          echo "PASS: setup-gradle completed with deprecated gradle-home-cache-cleanup input"
